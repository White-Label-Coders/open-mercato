import type { ModuleInfo } from '@open-mercato/shared/modules/registry'

export * from './types'

export const metadata: ModuleInfo = {
  name: 'voice_channels',
  title: 'Voice Channels',
  description: 'Voice Channels Hub + AI Call Copilot (hackathon MVP — frontend only).',
  version: '0.1.0',
}
